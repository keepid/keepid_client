language: java
jdk: oraclejdk13
dist: trusty
install: true
script:
  - mvn test -B
  - mvn cobertura:cobertura
  - bash <(curl -s https://codecov.io/bash) -t uuid-repo-token
cache:
  directories:
    - $HOME/.m2